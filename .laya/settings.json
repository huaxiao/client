// 将设置放入此文件中以覆盖默认值和用户设置。
{
    // 配置 glob 模式以排除文件和文件夹。
  "files.exclude": {
    "**/.laya": true,
    "**/release": true
    // "**/bin": true
  },

  "search.exclude": {
    "**/bin": true,
    "**/release": true
  }
}